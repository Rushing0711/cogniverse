## 1. 重命名函数

- [x] 1.1 将函数名 `intersections` 改为 `difference`，同步修改注释"去差值"为"差集"（第 12-13 行）
- [x] 1.2 更新调用点 `intersections(files, WHITE_LIST)` 为 `difference(files, WHITE_LIST)`（第 41 行）

## 2. 验证

- [x] 2.1 运行 `pnpm docs:build`，确认侧边栏生成正常，构建无报错
